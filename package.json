{
  "name": "claude-serif-patch",
  "version": "1.0.0",
  "private": true,
  "description": "Restore the Anthropic-Serif patch to Claude Desktop after updates.",
  "scripts": {
    "patch": "bash ./patch.sh"
  },
  "dependencies": {
    "@electron/asar": "^3.2.10"
  }
}
